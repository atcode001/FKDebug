# FKDebug 使用文档

## 界面概览

FKDebug 是一款 Android 调试辅助工具，主界面分为以下几个区域：

- **标题栏**：窗口控制按钮（最小化、最大化、关闭）
- **日志标签区**：local / frida / dmesg / logcat 四个日志标签页
- **日志列表面板**（左侧）：显示各来源的日志信息
- **详情面板**（日志与设备面板之间）：查看日志详情、编辑内容
- **设备进程面板**（右侧）：设备管理、进程控制、脚本管理、文件管理、FKLoader配置、小工具
- **状态栏**：MCP 服务状态、主题切换、字体大小

---

## 1. 标题栏

标题栏位于窗口顶部，显示软件名称，右侧提供三个窗口操作按钮：

| 按钮 | 功能 |
|------|------|
| **—** | 最小化窗口 |
| **□** | 最大化/还原窗口 |
| **✕** | 关闭窗口 |

- 双击标题栏空白区域可切换最大化/还原。
- 拖拽标题栏可移动窗口。

---

## 2. 日志标签页

主界面左侧上方为 TabControl，包含四个日志标签：

| 标签 | 说明 |
|------|------|
| **local** | 本地脚本日志输出 |
| **frida** | Frida 脚本日志输出 |
| **dmesg** | 内核日志（dmesg） |
| **logcat** | Android logcat 日志 |

点击标签切换对应的日志源。

---

## 3. 日志列表

每个日志标签页包含一个日志列表和一个底部过滤栏。

### 3.1 列表列说明

#### local / frida / dmesg 日志列

| 列 | 说明 |
|------|------|
| **LV** | 日志级别（点击表头下拉可过滤级别） |
| **#** | 日志序号 |
| **时间** | 日志时间 |
| **脚本类型** | 脚本类型（点击表头下拉可过滤） |
| **消息** | 日志内容（表头含搜索框和标签标题） |

#### logcat 日志列

| 列 | 说明 |
|------|------|
| **LV** | 日志级别（点击表头下拉可过滤级别） |
| **#** | 日志序号 |
| **时间** | 日志时间 |
| **PID-TID** | 进程/线程 ID（表头输入框可过滤） |
| **TAG** | 日志标签（表头输入框可过滤） |
| **包名** | 应用包名（表头输入框可过滤） |
| **消息** | 日志内容（表头含搜索框） |

### 3.2 快捷搜索

在日志列表表头的搜索框中输入关键词，可实时过滤日志内容。

### 3.3 右键菜单

#### local / frida / dmesg 日志右键菜单

| 菜单项 | 功能 |
|--------|------|
| **清空日志** | 清空当前列表所有日志 |
| **重新加载** | 重新加载日志 |
| **添加过滤** | 添加过滤条件 |
| **复制 → 全部复制** | 复制选中的日志行 |
| **复制 → 解析Json复制...** | 解析日志中的 JSON 并复制 |
| **保存 → 仅数据** | 保存日志数据 |
| **保存 → 完整日志** | 保存完整日志 |
| **整理 → 还原** | 还原日志排列 |
| **整理 → 去重** | 去除重复日志 |
| **整理 → 排序 → 按日志排序** | 按日志内容排序 |
| **整理 → 排序 → 按Json key排序...** | 按 JSON key 排序 |
| **打开日志文件** | 打开已保存的日志文件 |

#### logcat 日志右键菜单

| 菜单项 | 功能 |
|--------|------|
| **清空日志** | 清空当前列表所有日志 |
| **重新加载** | 重新加载日志 |
| **停止采集** | 停止 logcat 采集 |
| **开始采集** | 开始 logcat 采集 |
| **打开日志文件** | 打开已保存的日志文件 |

### 3.4 底部过滤栏

日志列表底部显示当前已启用的过滤器按钮：

- 点击过滤按钮可启用/切换过滤
- **右键点击过滤按钮**：
  - **编辑过滤器**：修改过滤器条件
  - **删除过滤器**：移除该过滤器

---

## 4. 详情面板

位于日志列表与设备面板之间，用于查看和编辑内容。

### 工具栏

| 按钮 | 功能 |
|------|------|
| **垃圾桶图标** | 清空详情面板内容 |
| **✕** | 关闭（仅在文件查看模式显示） |

### 右键菜单

| 菜单项 | 功能 |
|--------|------|
| **复制** | 复制选中内容 |
| **剪切** | 剪切选中内容 |
| **粘贴** | 粘贴内容 |
| **格式化Json** | 格式化面板中的 JSON 文本 |
| **保存到文件** | 保存内容到原文件 |
| **另存为...** | 另存内容到新文件 |

详情面板支持编辑，可通过手机文件查看功能加载远程文件内容到面板中编辑并保存回手机。

---

## 5. 设备进程面板（右侧）

右侧面板是 FKDebug 的核心功能区，包含 **调试**、**文件**、**工具** 三个子标签页。

### 5.1 通用操作

- **折叠按钮 ▶/◀**：点击可折叠/展开右侧面板
- 面板折叠后，仅显示精简的启动/重启/附加/剥离/加载/卸载/重载按钮

### 5.2 调试标签页

#### 设备选择区

- **设备下拉框**：选择连接的 Android 设备
- **刷新按钮 ⟳**：刷新设备列表和进程列表
- **root 输入框**：配置 su 命令名称（默认 `su`）
- **adb 输入框**：配置 adb 路径

#### Gadget 工具条

显示 Frida gadget 相关操作按钮：

| 按钮 | 功能 |
|------|------|
| **安装** | 安装应用 |
| **脚本模式** | 切换为 Spawn 启动模式 |
| **监听模式** | 切换为监听（Attach）模式 |
| **启动** | Spawn 启动进程 |
| **重启** | 重启任务 |
| **附加** | Attach 到进程 |
| **剥离** | 剥离脚本会话 |

#### 进程列表

- 显示设备上的进程列表，含包名和 PID
- **搜索框**：输入关键词过滤进程
- **调试进程**：显示当前设置的调试进程名
- **展开/收起按钮 ▲/▼**：切换进程列表显示/隐藏

**进程右键菜单**：

| 菜单项 | 功能 |
|--------|------|
| **设置为调试进程** | 将选中进程设为调试目标 |
| **结束进程** | 强制结束选中进程 |
| **清理数据** | 清理应用数据 |
| **启用FKLoader** | 启用 FKLoader |
| **禁用FKLoader** | 禁用 FKLoader |
| **刷新进程** | 刷新进程列表 |

#### 脚本区

- **展开/收起按钮 ▲/▼**：切换常用脚本列表显示/隐藏

**简单脚本**：以 CheckBox 形式显示，勾选即启用
**复杂脚本**：每个独立一行，包含：
  - 主 CheckBox：启用/禁用脚本
  - 参数控件：内含 CheckBox、Radio 选项、按钮等配

**自定义脚本**：
  - 勾选启用
  - 从下拉框选择自定义脚本文件

#### 加载/卸载/重载按钮

位于面板顶部折叠态显示区：

| 按钮 | 功能 |
|------|------|
| **加载** | 加载脚本 |
| **卸载** | 卸载脚本 |
| **重载** | 重新加载脚本 |

---

### 5.3 文件标签页

文件管理器，支持电脑与手机之间的文件推送和拉取。

#### 推送区（左）

- **源路径**：电脑文件路径
- **root 模式**：勾选后使用 root 权限推送
- **推送按钮**：执行推送操作
- **手机路径**：目标手机文件路径
- **推送列表**：显示推送记录（含自动推送勾选项）

**推送列表右键菜单**：

| 菜单项 | 功能 |
|--------|------|
| **删除** | 删除推送记录 |

#### 拉取区（右）

- **目标路径**：拉取到电脑的保存路径
- **拉取按钮**：执行拉取操作
- **手机路径**：手机源文件路径
- **拉取列表**：显示拉取记录

**拉取列表右键菜单**：

| 菜单项 | 功能 |
|--------|------|
| **删除** | 删除拉取记录 |

#### 文件浏览

左右两侧各有一个文件浏览器（路径导航 + 文件列表）。

**电脑文件列表右键菜单**：

| 菜单项 | 功能 |
|--------|------|
| **刷新** | 刷新文件列表 |
| **推送** | 推送选中文件到手机 |
| **在资源管理器打开** | 在 Windows 资源管理器中打开 |
| **查看文件** | 在详情面板中查看文件 |

**手机文件列表右键菜单**：

| 菜单项 | 功能 |
|--------|------|
| **刷新** | 刷新文件列表 |
| **拉取** | 拉取选中文件到电脑 |
| **查看文件** | 在详情面板中查看文件 |

#### 底部日志

文件管理区域底部显示推送/拉取操作日志。

---

### 5.4 常用文件

显示常用的推送文件列表（与文件管理器的推送列表同步）。

| 列 | 说明 |
|------|------|
| **自推** | 勾选后自动推送 |
| **电脑/手机文件** | 显示电脑文件和对应的手机目标路径 |

**右键菜单**：

| 菜单项 | 功能 |
|--------|------|
| **推送** | 推送选中文件 |
| **拉取** | 拉取选中文件 |
| **查看PC文件** | 在详情面板查看电脑文件 |
| **查看手机文件** | 在详情面板查看手机文件 |
| **删除** | 删除记录 |

---

### 5.5 FKLoader 配置

FKLoader 是内置的注入框架配置管理。

#### 未安装状态

显示提示信息和 **安装FKLoader** 按钮。

#### 已安装状态

**左侧**：已配置的目标应用列表

| 列 | 说明 |
|------|------|
| **启用** | 勾选启用该目标 |
| **包名** | 应用包名 |

**左侧右键菜单**：

| 菜单项 | 功能 |
|--------|------|
| **刷新** | 刷新配置 |
| **删除目标** | 删除选中目标 |
| **同步到手机** | 同步配置到手机 |

**右侧**：选中目标的注入库管理

- **库路径输入框**：输入要注入的库路径
- **添加按钮**：添加注入库

**库列表右键菜单**：

| 菜单项 | 功能 |
|--------|------|
| **删除库** | 删除选中库 |

---

### 5.6 小工具

在调试标签页底部，提供三个便捷工具入口：

| 按钮 | 功能 |
|------|------|
| **编码** | 打开编码转换工具窗口 |
| **时间戳** | 打开时间戳转换工具窗口 |
| **Hash** | 打开 Hash 计算工具窗口 |

---

## 6. 状态栏

位于窗口底部，提供以下功能：

### 6.1 MCP 服务状态

圆形指示灯显示 MCP 服务运行状态。右键点击弹出菜单：

| 菜单项 | 功能 |
|--------|------|
| **启动** | 启动 MCP 服务 |
| **停止** | 停止 MCP 服务 |
| **复制配置** | 复制 MCP 配置信息 |
| **设置端口...** | 设置 MCP 服务端口 |

### 6.2 主题切换

点击可弹出主题菜单，选择不同的界面主题配色。

### 6.3 字体大小

点击可弹出字体大小菜单，调整界面字体大小。

---

## 7. 工具窗口

### 7.1 编码转换

支持多种编码格式之间的互转，如 Base64、Hex、Unicode 等。

- **文本编码**：选择源文本的编码格式
- **源输入区**：输入需要转换的内容
- **目标输出区**：显示转换结果

### 7.2 时间戳转换

支持时间戳与日期时间之间的互转。

- **输入时间戳或日期时间**：自动识别并互转
- **当前时间按钮**：快速填入当前时间

### 7.3 Hash 计算

支持多种 Hash 算法（MD5、SHA1、SHA256、SHA512 等数十种算法）。

- **输入字符串**：手动输入文本计算 Hash
- **拖拽文件**：将文件拖入窗口自动计算 Hash
- 计算结果按算法分组件展示，支持点击复制

### 7.4 IDA 函数名提取

帮助从 IDA 导出数据中提取函数名。

- **左侧**：粘贴 SO 文件数据
- **右侧**：显示提取的函数名

### 7.5 SVC 搜索

在 SO 文件中搜索服务相关数据。

- **搜索按钮**：打开 SO 文件并执行搜索
- **结果显示区**：展示搜索结果

### 7.6 日志过滤

配置日志过滤条件。

| 字段 | 说明 |
|------|------|
| **过滤器名称** | 过滤器的名称 |
| **包含** | 包含的关键词 |
| **排除** | 排除的关键词 |

### 7.7 跳转行号

输入行号快速跳转到日志的指定行。

### 7.8 脚本参数配置

为复杂脚本配置参数。

- **提示信息**：显示参数说明
- **参数输入框**：输入参数值
- **确定/取消**：确认或取消参数配置

---

## 8. 快捷键

| 快捷键 | 功能 |
|--------|------|
| **Ctrl+C** | 复制选中内容 |
| **Ctrl+V** | 粘贴内容 |
| **Ctrl+X** | 剪切内容 |
| **Enter**（跳转行号窗口） | 确认跳转 |

---

## 注意事项

1. 首次启动时需选择工作文件夹，用于保存脚本、日志等数据。
2. 使用 ADB 功能前请确保 Android 设备已通过 USB 连接并开启调试模式。
3. FKLoader 配置修改后需点击 **同步到手机** 才能生效。
4. 日志搜索支持实时过滤，输入关键词即自动筛选。
